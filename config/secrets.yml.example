# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  secret_key_base: <put the result of rake secret here>

  mailer_username: <account used to send all mail>
  mailer_password: <password of mailer account>
  enquiry_mail_to: <address to receive enquiries mail>
  locker_mail_to: <address to receive locker mail>

  GMAIL_USERNAME: <outdated mailer account>
  GMAIL_PASSWORD: <password of outdated mailer account>
  IVLE_KEY: <ivle LAPI key>
  GOOGLE_CLIENT_ID: <google client id from google developer console>
  GOOGLE_CLIENT_SECRET: <google secret from google developer console>

test:
  secret_key_base: <put the result of rake secret here>

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <put the result of rake secret here>

  mailer_username: <account used to send all mail>
  mailer_password: <password of mailer account>
  enquiry_mail_to: <address to receive enquiries mail>
  locker_mail_to: <address to receive locker mail>

  GMAIL_USERNAME: <outdated mailer account>
  GMAIL_PASSWORD: <password of outdated mailer account>
  IVLE_KEY: <ivle LAPI key>
  GOOGLE_CLIENT_ID: <google client id from google developer console>
  GOOGLE_CLIENT_SECRET: <google secret from google developer console>
